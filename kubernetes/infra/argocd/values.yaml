syncWave: "-20"

tailscale:
  enabled: true
  hostname: argocd

argo-cd:
  # A complete list of values can be found here:
  # https://github.com/argoproj/argo-helm/blob/main/charts/argo-cd/values.yaml

  configs:
    secret:
      createSecret: false

    params:
      # insecure, but behind the tailscale (https) ingress
      server.insecure: true

  applicationSet:
    enabled: true
