argo-cd:
  # A complete list of values can be found here:
  # https://github.com/argoproj/argo-helm/blob/main/charts/argo-cd/values.yaml

  server:
    ingress:
      enabled: true
      ingressClassName: traefik  # <-- Traefik should watch this class
      hosts:
        - argocd.local
      paths:
        - /
      pathType: Prefix

  configs:
    params:
      server.insecure: true

  applicationSet:
    enabled: true
