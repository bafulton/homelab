namespace: tailscale
syncWave: "-2"

# Bitwarden secrets pulled via ExternalSecret
bitwarden-secret:
  secrets:
    - name: operator-oauth
      data:
        client_id: "0444abc5-ca33-41a7-a90d-b3c70174b122"
        client_secret: "8a0781d1-8c35-42f9-8b6b-b3c70174c7e6"

tailscale-operator:
  # A complete list of values can be found here:
  # https://github.com/tailscale/tailscale/blob/main/cmd/k8s-operator/deploy/chart/values.yaml

  # Do NOT set oauth.clientId / oauth.clientSecret — leaving this empty
  # tells the chart to use the existing Secret named "operator-oauth".
  oauth: {}

  operatorConfig:
    # ACL tag that operator will be tagged with. The operator must be made
    # owner of these tags.
    # (This is default, but it's good to make it explicit.)
    defaultTags:
      - "tag:k8s-operator"
