apiVersion: v2
name: descheduler
description: Wrapper Helm chart for the Kubernetes descheduler (rebalances pods off over-utilized nodes)
type: application
version: 1.0.0
appVersion: "0.36.0"
dependencies:
  - name: descheduler
    # renovate: datasource=helm registryUrl=https://kubernetes-sigs.github.io/descheduler/
    version: 0.36.0
    repository: https://kubernetes-sigs.github.io/descheduler/
