apiVersion: v2
name: volsync
description: VolSync asynchronous volume replication for PVC backups
type: application
version: 1.0.0
appVersion: "0.15.0"
dependencies:
  - name: volsync
    # renovate: datasource=helm registryUrl=https://backube.github.io/helm-charts/
    version: 0.15.0
    repository: https://backube.github.io/helm-charts/
