syncWave: "0"

claimData: &claim
  token: "Wxz8Pc3J35qyanIzNDuH17QIJkbcjXQMVnB6BLSkT3bpNKYZmzz6cNVVkNNasv-M"
  rooms: "3733cd36-6305-45e9-af5b-d172ca1ff4b7"

netdata:
  # A complete list of values can be found here:
  # https://github.com/netdata/helmchart/blob/master/charts/netdata/values.yaml

  ingress:
    enabled: false

  sd:
    child:
      enabled: false

  parent:
    enabled: false

  child:
    resources:
      requests:
        cpu: 250m
        memory: 200Mi
      limits:
        memory: 300Mi

    configs:
      stream:
        enabled: false

    claiming:
      enabled: true
      <<: *claim

  k8sState:
    resources:
      requests:
        cpu: 100m
        memory: 100Mi
      limits:
        memory: 200Mi

    configs:
      stream:
        enabled: false

    claiming:
      enabled: true
      <<: *claim
