{{- if and .Values.adminUser.enabled .Values.adminUser.longLived }}
apiVersion: v1
kind: Secret
metadata:
  name: kube-dashboard-admin
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/service-account.name: kube-dashboard-admin
type: kubernetes.io/service-account-token  
{{- end }}
